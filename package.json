{
  "name": "vortex-ai-site",
  "version": "1.0.0",
  "private": true,
  "description": "Vórtex AI web application",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8080"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
